# 🗺️ Roadmap мікросервісів

## ✅ Вже створено

1. **Auth Service** ✅
   - Реєстрація/вхід
   - OAuth (Google, Microsoft)
   - Email верифікація
   - JWT токени

2. **User Service** ✅
   - Профілі користувачів
   - Пошук користувачів
   - Дружба (Social Features)

---

## 📋 Сервіси, які потрібно створити

### 🔴 Пріоритет 1: Основні бізнес-сервіси (найважливіші)

#### 3. **Project Service** 🎯
**Порт:** 3004
**Відповідальність:**
- CRUD проєктів
- Шаблони проєктів
- Прогрес проєктів
- Аналіз проєктів (інтеграція з AI Service)
- Статистика проєктів

**API Endpoints:**
- `GET /api/projects` - список проєктів
- `GET /api/projects/:id` - деталі проєкту
- `POST /api/projects` - створити проєкт
- `PUT /api/projects/:id` - оновити проєкт
- `DELETE /api/projects/:id` - видалити проєкт
- `GET /api/projects/templates` - шаблони проєктів
- `POST /api/projects/from-template` - створити з шаблону
- `GET /api/projects/:id/stats` - статистика проєкту

**Залежності:**
- User Service (для отримання інформації про власника)
- AI Service (для аналізу проєктів)

---

#### 4. **Task Service** 🎯
**Порт:** 3005
**Відповідальність:**
- CRUD завдань
- Підзавдання
- Залежності між завданнями
- Статуси та пріоритети
- Gantt діаграми
- Відпрацьовані години

**API Endpoints:**
- `GET /api/tasks` - список завдань
- `GET /api/tasks/:id` - деталі завдання
- `POST /api/tasks` - створити завдання
- `PUT /api/tasks/:id` - оновити завдання
- `DELETE /api/tasks/:id` - видалити завдання
- `GET /api/tasks/:id/subtasks` - підзавдання
- `POST /api/tasks/:id/subtasks` - створити підзавдання
- `GET /api/tasks/:id/dependencies` - залежності
- `GET /api/tasks/:id/worked-hours` - відпрацьовані години
- `GET /api/tasks/gantt` - дані для Gantt діаграми

**Залежності:**
- Project Service (завдання належать проєктам)
- User Service (для отримання інформації про призначених)
- Time Service (для відпрацьованих годин)

---

#### 5. **Team Service** 🎯
**Порт:** 3006
**Відповідальність:**
- Створення команд
- Учасники команд
- Ролі та права доступу
- Командні проєкти

**API Endpoints:**
- `GET /api/teams` - список команд
- `GET /api/teams/:id` - деталі команди
- `POST /api/teams` - створити команду
- `PUT /api/teams/:id` - оновити команду
- `DELETE /api/teams/:id` - видалити команду
- `GET /api/teams/:id/members` - учасники команди
- `POST /api/teams/:id/members` - додати учасника
- `DELETE /api/teams/:id/members/:userId` - видалити учасника
- `PUT /api/teams/:id/members/:userId/role` - змінити роль

**Залежності:**
- User Service (для отримання інформації про учасників)
- Project Service (командні проєкти)

---

### 🟡 Пріоритет 2: Спеціалізовані сервіси

#### 6. **Financial Service** 💰
**Порт:** 3007
**Відповідальність:**
- Інвойси
- Регулярні інвойси
- Платежі (Stripe)
- Витрати
- Бюджет трекінг
- Cash Flow аналітика

**API Endpoints:**
- `GET /api/invoices` - список інвойсів
- `GET /api/invoices/:id` - деталі інвойсу
- `POST /api/invoices` - створити інвойс
- `PUT /api/invoices/:id` - оновити інвойс
- `POST /api/invoices/:id/pay` - оплатити інвойс
- `GET /api/expenses` - список витрат
- `POST /api/expenses` - додати витрату
- `GET /api/budget` - бюджет проєкту
- `GET /api/cashflow` - cash flow аналітика
- `GET /api/recurring-invoices` - регулярні інвойси

**Залежності:**
- Project Service (інвойси та витрати належать проєктам)
- Stripe API (для платежів)

---

#### 7. **Chat Service** 💬
**Порт:** 3008
**Відповідальність:**
- Direct чати
- Групові чати
- Проєктні чати
- Real-time повідомлення (WebSocket)
- Реакції на повідомлення
- Вкладення файлів

**API Endpoints:**
- `GET /api/chat` - список чатів
- `GET /api/chat/:id` - деталі чату
- `POST /api/chat` - створити чат
- `GET /api/chat/:id/messages` - повідомлення
- `POST /api/chat/:id/messages` - надіслати повідомлення
- `PUT /api/chat/messages/:id` - редагувати повідомлення
- `DELETE /api/chat/messages/:id` - видалити повідомлення
- `POST /api/chat/messages/:id/reactions` - додати реакцію
- `POST /api/chat/messages/:id/read` - позначити як прочитане

**WebSocket Events:**
- `message.new` - нове повідомлення
- `message.updated` - повідомлення оновлено
- `message.deleted` - повідомлення видалено
- `typing.start` - користувач друкує
- `typing.stop` - користувач перестав друкувати

**Залежності:**
- User Service (для отримання інформації про учасників)
- Project Service (проєктні чати)
- File Service (вкладення файлів)

---

#### 8. **File Service** 📁
**Порт:** 3009
**Відповідальність:**
- Завантаження файлів
- Версіонування файлів
- Прив'язка до проєктів/завдань
- S3/R2 інтеграція
- Генерація прев'ю

**API Endpoints:**
- `GET /api/files` - список файлів
- `GET /api/files/:id` - деталі файлу
- `POST /api/files/upload` - завантажити файл
- `GET /api/files/:id/download` - завантажити файл
- `DELETE /api/files/:id` - видалити файл
- `GET /api/files/:id/versions` - версії файлу
- `GET /api/files/:id/preview` - прев'ю файлу

**Залежності:**
- Project Service (файли належать проєктам)
- Task Service (файли прив'язані до завдань)
- AWS S3 / Cloudflare R2 (зберігання)

---

#### 9. **Time Tracking Service** ⏱️
**Порт:** 3010
**Відповідальність:**
- Time entries
- Clock in/out
- Відпрацьовані години
- Звіти по часу

**API Endpoints:**
- `GET /api/time-entries` - список записів часу
- `POST /api/time-entries` - створити запис
- `PUT /api/time-entries/:id` - оновити запис
- `DELETE /api/time-entries/:id` - видалити запис
- `POST /api/time-entries/clock-in` - почати роботу
- `POST /api/time-entries/clock-out` - закінчити роботу
- `GET /api/time-entries/reports` - звіти по часу

**Залежності:**
- User Service (для отримання інформації про користувачів)
- Task Service (записи часу належать завданням)
- Project Service (записи часу належать проєктам)

---

### 🟢 Пріоритет 3: Допоміжні сервіси

#### 10. **Notification Service** 🔔
**Порт:** 3011
**Відповідальність:**
- In-app сповіщення
- Email сповіщення
- Різні типи подій
- Налаштування сповіщень

**API Endpoints:**
- `GET /api/notifications` - список сповіщень
- `PUT /api/notifications/:id/read` - позначити як прочитане
- `PUT /api/notifications/read-all` - позначити всі як прочитані
- `DELETE /api/notifications/:id` - видалити сповіщення
- `GET /api/notifications/settings` - налаштування сповіщень
- `PUT /api/notifications/settings` - оновити налаштування

**Події (слухає):**
- `user.registered`
- `task.created`
- `task.assigned`
- `project.created`
- `message.new`
- `comment.new`
- `invoice.paid`

**Залежності:**
- User Service (для отримання інформації про користувачів)
- Event Bus (для отримання подій)
- Email Service (Resend)

---

#### 11. **Comments Service** 💬
**Порт:** 3012
**Відповідальність:**
- Коментарі до завдань/проєктів
- Вкладені коментарі
- Меншени (@username)
- Резолюція коментарів

**API Endpoints:**
- `GET /api/comments` - список коментарів
- `GET /api/comments/:id` - деталі коментаря
- `POST /api/comments` - створити коментар
- `PUT /api/comments/:id` - оновити коментар
- `DELETE /api/comments/:id` - видалити коментар
- `POST /api/comments/:id/resolve` - вирішити коментар
- `GET /api/comments/:id/replies` - відповіді на коментар

**Залежності:**
- User Service (для отримання інформації про авторів)
- Project Service (коментарі до проєктів)
- Task Service (коментарі до завдань)

---

#### 12. **Analytics Service** 📊
**Порт:** 3013
**Відповідальність:**
- Генерація звітів
- PDF експорт
- Статистика
- Аналітика продуктивності

**API Endpoints:**
- `GET /api/analytics/stats` - загальна статистика
- `GET /api/analytics/projects` - статистика проєктів
- `GET /api/analytics/tasks` - статистика завдань
- `GET /api/analytics/time` - статистика часу
- `GET /api/analytics/financial` - фінансова статистика
- `POST /api/analytics/reports` - створити звіт
- `GET /api/analytics/reports/:id` - отримати звіт
- `GET /api/analytics/reports/:id/pdf` - експорт в PDF

**Залежності:**
- Project Service
- Task Service
- Time Service
- Financial Service
- ClickHouse / TimescaleDB (для аналітики)

---

#### 13. **AI Service** 🤖
**Порт:** 3014
**Відповідальність:**
- Аналіз проєктів
- Генерація рекомендацій
- Автоматизація завдань
- AI-асистент

**API Endpoints:**
- `POST /api/ai/analyze-project` - проаналізувати проєкт
- `POST /api/ai/generate-tasks` - згенерувати завдання
- `POST /api/ai/chat` - AI-асистент
- `POST /api/ai/suggestions` - рекомендації

**Залежності:**
- Project Service (для аналізу проєктів)
- Anthropic Claude API
- OpenAI API (опціонально)

---

## 📊 Статистика

- **Всього сервісів:** 13
- **Створено:** 2 (Auth, User)
- **Залишилося:** 11

## 🎯 Рекомендований порядок створення

1. ✅ **Auth Service** - готово
2. ✅ **User Service** - готово
3. 🎯 **Project Service** - наступний (базовий для інших)
4. 🎯 **Task Service** - залежить від Project
5. 🎯 **Team Service** - залежить від Project
6. 💰 **Financial Service** - залежить від Project
7. 💬 **Chat Service** - залежить від Project, User
8. 📁 **File Service** - незалежний, але використовується багатьма
9. ⏱️ **Time Tracking Service** - залежить від Task, Project
10. 🔔 **Notification Service** - слухає події від інших сервісів
11. 💬 **Comments Service** - залежить від Project, Task
12. 📊 **Analytics Service** - залежить від багатьох сервісів
13. 🤖 **AI Service** - залежить від Project

---

## 📝 Примітки

- Всі сервіси використовують спільну базу даних (Neon PostgreSQL) на початку
- Пізніше можна виділити окремі бази для кожного сервісу
- Chat Service може використовувати MongoDB для кращої продуктивності
- Analytics Service може використовувати ClickHouse або TimescaleDB

