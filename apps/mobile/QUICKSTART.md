# 🚀 Швидкий старт - 5 хвилин

## Крок 1: Встановлення (2 хв)

```bash
cd mobile
npm install
```

## Крок 2: Налаштування (1 хв)

```bash
# Створіть .env файл
cp .env.example .env
```

**Відредагуйте `.env`:**

### Для емулятора/симулятора:
```env
EXPO_PUBLIC_AUTH_SERVICE_URL=http://localhost:3002
```

### Для реального телефону:
```env
# Дізнайтесь ваш IP:
# macOS: ifconfig | grep "inet " | grep -v 127.0.0.1
# Windows: ipconfig

# Замініть XXX на ваш IP:
EXPO_PUBLIC_AUTH_SERVICE_URL=http://192.168.1.XXX:3002
```

## Крок 3: Запуск (2 хв)

```bash
npm start
```

### Тестування на телефоні:

1. **Встановіть Expo Go:**
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. **Скануйте QR код:**
   - iOS: Відкрийте Camera → скануйте
   - Android: Відкрийте Expo Go → скануйте

### Або на емуляторі:

```bash
# iOS (тільки macOS)
npm run ios

# Android
npm run android
```

## ✅ Готово!

Тепер можете:
- ✅ Зареєструватись
- ✅ Увійти в систему
- ✅ Переглянути профіль
- ✅ Тестувати навігацію

## 📚 Детальна інформація

- **Повна документація:** `README.md`
- **Інструкція з тестування:** `TESTING.md`
- **Підсумок проекту:** `SUMMARY.md`

## 🐛 Проблеми?

### "Network request failed"
1. Перевірте що бекенд запущений (auth-service на порту 3002)
2. Використовуйте IP адресу замість localhost в `.env`
3. Телефон і комп'ютер мають бути в одній Wi-Fi мережі

### "Unable to connect"
```bash
npx expo start --tunnel
```

**Успіхів! 🎉**
