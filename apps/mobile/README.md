# 📱 Project Scope Analyzer - Mobile App

React Native мобільний додаток на базі Expo для Project Scope Analyzer.

## 🚀 Швидкий старт

### Передумови

- Node.js 18+ 
- npm або yarn
- Expo Go app на вашому телефоні ([iOS](https://apps.apple.com/app/expo-go/id982107779) | [Android](https://play.google.com/store/apps/details?id=host.exp.exponent))
- Для iOS розробки: macOS з Xcode
- Для Android розробки: Android Studio

### Встановлення

1. **Встановіть залежності:**
   ```bash
   cd mobile
   npm install
   ```

2. **Створіть файл `.env`:**
   ```bash
   cp .env.example .env
   ```

3. **Налаштуйте змінні оточення в `.env`:**
   ```env
   EXPO_PUBLIC_AUTH_SERVICE_URL=http://localhost:3002
   # Для тестування на реальному пристрої використовуйте IP вашого комп'ютера:
   # EXPO_PUBLIC_AUTH_SERVICE_URL=http://192.168.1.XXX:3002
   ```

### Запуск

```bash
# Запустити Expo development server
npm start

# Запустити на iOS симуляторі (тільки macOS)
npm run ios

# Запустити на Android емуляторі
npm run android

# Запустити веб версію
npm run web
```

## 📱 Тестування на реальному пристрої

### Варіант 1: Expo Go (Рекомендовано для розробки)

1. **Встановіть Expo Go:**
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. **Запустіть development server:**
   ```bash
   npm start
   ```

3. **Скануйте QR код:**
   - **iOS**: Відкрийте Camera app і скануйте QR код
   - **Android**: Відкрийте Expo Go app і скануйте QR код

4. **Важливо для тестування з бекендом:**
   - Переконайтесь, що ваш телефон і комп'ютер в одній Wi-Fi мережі
   - Використовуйте IP адресу вашого комп'ютера в `.env`:
     ```env
     EXPO_PUBLIC_AUTH_SERVICE_URL=http://192.168.1.XXX:3002
     ```
   - Дізнайтесь ваш IP:
     - macOS: `ifconfig | grep "inet " | grep -v 127.0.0.1`
     - Windows: `ipconfig`

### Варіант 2: Development Build

Для доступу до нативних модулів, які не підтримуються в Expo Go:

```bash
# Створити development build
npx expo run:ios
# або
npx expo run:android
```

### Варіант 3: Production Build

```bash
# iOS
eas build --platform ios

# Android
eas build --platform android
```

## 🏗️ Структура проекту

```
mobile/
├── app/                    # Expo Router файли
│   ├── (auth)/            # Екрани аутентифікації
│   │   ├── login.tsx
│   │   └── signup.tsx
│   ├── (tabs)/            # Головні екрани з табами
│   │   ├── dashboard.tsx
│   │   ├── projects.tsx
│   │   └── profile.tsx
│   ├── _layout.tsx        # Root layout
│   └── index.tsx          # Entry point
├── components/            # Переіспользуємі компоненти
├── hooks/                 # Custom React hooks
│   └── useAuth.ts        # Хук аутентифікації
├── lib/                   # Утиліти та сервіси
│   └── auth-service.ts   # API клієнт для auth service
├── constants/             # Константи додатку
└── assets/               # Зображення, шрифти, тощо
```

## 🔧 Конфігурація

### Environment Variables

Створіть `.env` файл:

```env
# Backend Services
EXPO_PUBLIC_AUTH_SERVICE_URL=http://localhost:3002
EXPO_PUBLIC_USER_SERVICE_URL=http://localhost:3003
EXPO_PUBLIC_PROJECT_SERVICE_URL=http://localhost:3004

# Для тестування на реальному пристрої замініть localhost на IP:
# EXPO_PUBLIC_AUTH_SERVICE_URL=http://192.168.1.100:3002
```

### app.json

Основна конфігурація Expo додатку знаходиться в `app.json`.

## 🔗 Інтеграція з бекендом

Мобільний додаток використовує ті самі мікросервіси, що і веб-додаток:

- **Auth Service** (port 3002): Аутентифікація та авторизація
- **User Service** (port 3003): Управління користувачами
- **Project Service** (port 3004): Управління проектами
- **Task Service** (port 3005): Управління задачами
- **Team Service** (port 3006): Управління командами

### Спільний код

Мобільний додаток використовує спільний код з веб-додатку через папку `../shared/`:

- Типи TypeScript
- Схеми валідації
- Утиліти
- Константи

## 🧪 Тестування

```bash
# Запустити тести
npm test

# Запустити тести з coverage
npm test -- --coverage
```

## 📝 Основні функції

### ✅ Реалізовано

- ✅ Аутентифікація (Login/Signup)
- ✅ Збереження сесії (AsyncStorage)
- ✅ Профіль користувача
- ✅ Logout
- ✅ Tab Navigation
- ✅ Інтеграція з Auth Service

### 🚧 В розробці

- 🚧 Список проектів
- 🚧 Деталі проекту
- 🚧 Управління задачами
- 🚧 Push notifications
- 🚧 Offline mode

## 🐛 Troubleshooting

### Проблема: "Unable to connect to development server"

**Рішення:**
1. Переконайтесь, що телефон і комп'ютер в одній Wi-Fi мережі
2. Перевірте firewall налаштування
3. Спробуйте використати tunnel: `npx expo start --tunnel`

### Проблема: "Network request failed" при логіні

**Рішення:**
1. Перевірте, що бекенд сервіси запущені
2. Використовуйте IP адресу замість localhost в `.env`
3. Перевірте, що порти не заблоковані firewall

### Проблема: TypeScript помилки

**Рішення:**
```bash
# Очистити кеш та перевстановити
rm -rf node_modules
npm install
```

## 📚 Корисні посилання

- [Expo Documentation](https://docs.expo.dev/)
- [React Native Documentation](https://reactnative.dev/)
- [Expo Router Documentation](https://expo.github.io/router/docs/)
- [React Query Documentation](https://tanstack.com/query/latest)

## 🤝 Контрибуція

1. Створіть feature branch
2. Зробіть зміни
3. Протестуйте на iOS та Android
4. Створіть Pull Request

## 📄 Ліцензія

Цей проект є частиною Project Scope Analyzer.
