{
  "version": 2,
  "buildCommand": "cd ../.. && npm run build:shared && cd services/auth-service && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "memory": 1024
    }
  }
}

