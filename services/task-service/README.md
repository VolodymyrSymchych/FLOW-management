# Task Service 📋

Мікросервіс для управління завданнями в Project Scope Analyzer.

## Порт

**3005**

## Відповідальність

- CRUD завдань
- Підзавдання
- Залежності між завданнями
- Статуси та пріоритети
- Gantt діаграми
- Відпрацьовані години

## API Endpoints

### Завдання

- `GET /api/tasks` - список завдань
- `GET /api/tasks/:id` - деталі завдання
- `POST /api/tasks` - створити завдання
- `PUT /api/tasks/:id` - оновити завдання
- `DELETE /api/tasks/:id` - видалити завдання

### Підзавдання

- `GET /api/tasks/:id/subtasks` - підзавдання
- `POST /api/tasks/:id/subtasks` - створити підзавдання

### Залежності та Gantt

- `GET /api/tasks/:id/dependencies` - залежності
- `GET /api/tasks/gantt` - дані для Gantt діаграми

## Залежності

- **Project Service** (завдання належать проєктам)
- **User Service** (для отримання інформації про призначених)
- **Time Service** (для відпрацьованих годин)

## Встановлення

```bash
# Встановити залежності
npm install

# Налаштувати змінні оточення
cp .env.example .env

# Редагувати .env з вашими налаштуваннями
```

## Запуск

```bash
# Розробка
npm run dev

# Продакшн
npm run build
npm start
```

## База даних

Сервіс використовує спільну базу даних з таблицями:
- `tasks` - завдання
- `task_dependencies` - залежності між завданнями
- `time_entries` - відпрацьовані години

## Deployment на Vercel

Сервіс налаштований для деплою на Vercel як serverless function.

```bash
vercel --prod
```

## Аутентифікація

Всі ендпоінти (окрім health та metrics) вимагають JWT токен в заголовку:

```
Authorization: Bearer <token>
```

## Структура

```
task-service/
├── api/                  # Vercel serverless entry point
│   └── index.ts
├── src/
│   ├── config/          # Конфігурація
│   ├── controllers/     # HTTP контролери
│   ├── db/              # База даних (Drizzle ORM)
│   ├── middleware/      # Express middleware
│   ├── routes/          # API маршрути
│   ├── services/        # Бізнес-логіка
│   ├── types/           # TypeScript типи
│   ├── utils/           # Утиліти
│   ├── app.ts           # Express додаток
│   └── index.ts         # Точка входу
├── package.json
├── tsconfig.json
└── vercel.json
```
