# Team Service 🎯

Мікросервіс для управління командами та їх учасниками в Project Scope Analyzer.

## Порт

**3006**

## Відповідальність

- Створення команд
- Управління учасниками команд
- Ролі та права доступу
- Командні проєкти

## API Endpoints

### Команди

- `GET /api/teams` - список команд користувача
- `GET /api/teams/:id` - деталі команди
- `POST /api/teams` - створити команду
- `PUT /api/teams/:id` - оновити команду
- `DELETE /api/teams/:id` - видалити команду

### Учасники команд

- `GET /api/teams/:id/members` - учасники команди
- `POST /api/teams/:id/members` - додати учасника
- `DELETE /api/teams/:id/members/:userId` - видалити учасника
- `PUT /api/teams/:id/members/:userId/role` - змінити роль

## Залежності

- **User Service** (для отримання інформації про учасників)
- **Project Service** (командні проєкти)

## Встановлення

```bash
# Встановити залежності
npm install

# Налаштувати змінні оточення
cp .env.example .env

# Редагувати .env з вашими налаштуваннями
```

## Запуск

```bash
# Розробка
npm run dev

# Продакшн
npm run build
npm start
```

## База даних

Сервіс використовує спільну базу даних з таблицями:
- `teams` - команди
- `team_members` - учасники команд
- `team_projects` - зв'язок команд з проєктами

## Deployment на Vercel

Сервіс налаштований для деплою на Vercel як serverless function.

```bash
vercel --prod
```

## Аутентифікація

Всі ендпоінти (окрім health та metrics) вимагають JWT токен в заголовку:

```
Authorization: Bearer <token>
```

## Структура

```
team-service/
├── api/                  # Vercel serverless entry point
│   └── index.ts
├── src/
│   ├── config/          # Конфігурація
│   ├── controllers/     # HTTP контролери
│   ├── db/              # База даних (Drizzle ORM)
│   ├── middleware/      # Express middleware
│   ├── routes/          # API маршрути
│   ├── services/        # Бізнес-логіка
│   ├── types/           # TypeScript типи
│   ├── utils/           # Утиліти
│   ├── app.ts           # Express додаток
│   └── index.ts         # Точка входу
├── package.json
├── tsconfig.json
└── vercel.json
```
